<%= link_to food, id: dom_id(food), class: "text-decoration-none text-body" do %>
  <div class="d-flex w-100 justify-content-between align-items-baseline mb-2">
    <div class="fs-2"><%= food.name %></div>
  </div>
<% end %>
